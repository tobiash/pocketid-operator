# permissions for end users to view pocketidusers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: pocketid-operator
    app.kubernetes.io/managed-by: kustomize
  name: pocketiduser-viewer-role
rules:
- apiGroups:
  - pocketid.pocketid.tobiash.github.io
  resources:
  - pocketidusers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - pocketid.pocketid.tobiash.github.io
  resources:
  - pocketidusers/status
  verbs:
  - get
