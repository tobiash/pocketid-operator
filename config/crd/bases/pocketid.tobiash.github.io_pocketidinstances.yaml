---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pocketidinstances.pocketid.tobiash.github.io
spec:
  group: pocketid.tobiash.github.io
  names:
    kind: PocketIDInstance
    listKind: PocketIDInstanceList
    plural: pocketidinstances
    singular: pocketidinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.appUrl
      name: URL
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PocketIDInstance is the Schema for the pocketidinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PocketIDInstanceSpec defines the desired state of PocketIDInstance
            properties:
              allowedReferences:
                description: |-
                  AllowedReferences defines which namespaces can reference this instance
                  Similar to Gateway API's allowedRoutes pattern
                properties:
                  namespaces:
                    description: Namespaces indicates which namespaces are allowed
                      to reference this instance
                    properties:
                      from:
                        default: Same
                        description: From indicates where namespaces are selected
                        enum:
                        - All
                        - Same
                        - Selector
                        type: string
                      selector:
                        description: |-
                          Selector matches namespaces by label
                          Only valid when From=Selector
                        properties:
                          matchExpressions:
                            description: matchExpressions is a list of label selector
                              requirements. The requirements are ANDed.
                            items:
                              description: |-
                                A label selector requirement is a selector that contains values, a key, and an operator that
                                relates the key and values.
                              properties:
                                key:
                                  description: key is the label key that the selector
                                    applies to.
                                  type: string
                                operator:
                                  description: |-
                                    operator represents a key's relationship to a set of values.
                                    Valid operators are In, NotIn, Exists and DoesNotExist.
                                  type: string
                                values:
                                  description: |-
                                    values is an array of string values. If the operator is In or NotIn,
                                    the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                    the values array must be empty. This array is replaced during a strategic
                                    merge patch.
                                  items:
                                    type: string
                                  type: array
                                  x-kubernetes-list-type: atomic
                              required:
                              - key
                              - operator
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: |-
                              matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                              map is equivalent to an element of matchExpressions, whose key field is "key", the
                              operator is "In", and the values array contains only "value". The requirements are ANDed.
                            type: object
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                type: object
              appUrl:
                description: AppURL is the public URL where PocketID will be accessible
                  (e.g., https://auth.example.com)
                pattern: ^https?://
                type: string
              database:
                description: Database configuration
                properties:
                  postgresSecretRef:
                    description: |-
                      PostgresSecretRef references a Secret containing PostgreSQL connection details
                      Required when provider is "postgres"
                    properties:
                      name:
                        description: Name of the referenced object
                        type: string
                    required:
                    - name
                    type: object
                  provider:
                    default: sqlite
                    description: 'Provider is the database type: sqlite or postgres'
                    enum:
                    - sqlite
                    - postgres
                    type: string
                type: object
              encryptionKeySecretRef:
                description: |-
                  EncryptionKeySecretRef references a Secret containing the ENCRYPTION_KEY
                  If not set, operator will auto-generate one
                properties:
                  key:
                    description: Key within the Secret
                    type: string
                  name:
                    description: Name of the Secret
                    type: string
                required:
                - key
                - name
                type: object
              image:
                default: ghcr.io/pocket-id/pocket-id:latest
                description: Image specifies the PocketID container image to use
                type: string
              initialAdmin:
                description: |-
                  InitialAdmin configuration for the first user
                  Only used during initial setup
                properties:
                  displayName:
                    type: string
                  email:
                    type: string
                  firstName:
                    type: string
                  username:
                    type: string
                required:
                - displayName
                - email
                - firstName
                - username
                type: object
              replicas:
                default: 1
                description: Replicas is the number of PocketID instances to run
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines the resource requirements for the PocketID
                  container
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              sessionDuration:
                default: 60
                description: 'SessionDuration in minutes (default: 60)'
                type: integer
              smtp:
                description: SMTP configuration for email sending
                properties:
                  from:
                    description: From is the sender email address
                    type: string
                  host:
                    description: Host is the SMTP server hostname
                    type: string
                  port:
                    default: 587
                    description: Port is the SMTP server port
                    type: integer
                  secretRef:
                    description: SecretRef contains SMTP username and password
                    properties:
                      name:
                        description: Name of the referenced object
                        type: string
                    required:
                    - name
                    type: object
                  tls:
                    default: true
                    description: TLS enables STARTTLS
                    type: boolean
                required:
                - from
                - host
                type: object
              staticApiKeySecretRef:
                description: |-
                  StaticAPIKeySecretRef references a Secret containing the STATIC_API_KEY
                  Used by the operator to manage PocketID resources via API
                  If not set, operator will auto-generate one
                properties:
                  key:
                    description: Key within the Secret
                    type: string
                  name:
                    description: Name of the Secret
                    type: string
                required:
                - key
                - name
                type: object
              storage:
                description: Storage configuration for data persistence
                properties:
                  pvc:
                    description: PersistentVolumeClaim configuration for local storage
                    properties:
                      size:
                        default: 1Gi
                        description: Size of the PVC (e.g., "1Gi")
                        type: string
                      storageClassName:
                        description: StorageClassName for the PVC
                        type: string
                    type: object
                  s3:
                    description: S3 configuration for object storage
                    properties:
                      bucket:
                        description: Bucket name
                        type: string
                      endpoint:
                        description: Endpoint for S3-compatible storage
                        type: string
                      region:
                        description: Region
                        type: string
                      secretRef:
                        description: SecretRef contains access key and secret key
                        properties:
                          name:
                            description: Name of the referenced object
                            type: string
                        required:
                        - name
                        type: object
                    required:
                    - bucket
                    - endpoint
                    - secretRef
                    type: object
                type: object
              trustProxy:
                default: true
                description: TrustProxy enables trusting X-Forwarded-* headers
                type: boolean
            required:
            - appUrl
            type: object
          status:
            description: PocketIDInstanceStatus defines the observed state of PocketIDInstance
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the instance's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              internalUrl:
                description: InternalURL is the cluster-internal URL for API access
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              ready:
                description: Ready indicates if the instance is ready to accept requests
                type: boolean
              staticApiKeySecretName:
                description: StaticAPIKeySecretName is the name of the Secret containing
                  the static API key
                type: string
              version:
                description: Version is the detected PocketID version
                type: string
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
