---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pocketidusergroups.pocketid.tobiash.github.io
spec:
  group: pocketid.tobiash.github.io
  names:
    kind: PocketIDUserGroup
    listKind: PocketIDUserGroupList
    plural: pocketidusergroups
    singular: pocketidusergroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.isDefault
      name: Default
      type: boolean
    - jsonPath: .status.userCount
      name: Users
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PocketIDUserGroup is the Schema for the pocketidusergroups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PocketIDUserGroupSpec defines the desired state of PocketIDUserGroup
            properties:
              customClaims:
                description: CustomClaims to add to tokens for users in this group
                items:
                  description: CustomClaim defines a custom claim to add to tokens
                  properties:
                    key:
                      description: Key is the claim key
                      type: string
                    value:
                      description: Value is the claim value
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              friendlyName:
                description: FriendlyName is an optional human-friendly name
                type: string
              instanceRef:
                description: |-
                  InstanceRef references the PocketIDInstance this group belongs to
                  Can reference instances in other namespaces if allowed by the instance
                properties:
                  name:
                    description: Name of the referenced object
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced object
                      When unspecified, refers to the local namespace
                    type: string
                required:
                - name
                type: object
              isDefault:
                default: false
                description: IsDefault makes this group automatically assigned to
                  new users
                type: boolean
              name:
                description: Name is the display name of the group
                type: string
            required:
            - instanceRef
            - name
            type: object
          status:
            description: PocketIDUserGroupStatus defines the observed state of PocketIDUserGroup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              groupId:
                description: GroupID is the PocketID internal group ID
                type: string
              lastSyncTime:
                description: LastSyncTime is when the group was last synchronized
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              ready:
                description: Ready indicates if the group is synced with PocketID
                type: boolean
              synced:
                description: Synced indicates the group is successfully synchronized
                type: boolean
              userCount:
                description: UserCount is the number of users in this group
                type: integer
            required:
            - ready
            - synced
            - userCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
