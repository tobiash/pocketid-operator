---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: pocketidusers.pocketid.tobiash.github.io
spec:
  group: pocketid.tobiash.github.io
  names:
    kind: PocketIDUser
    listKind: PocketIDUserList
    plural: pocketidusers
    singular: pocketiduser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.username
      name: Username
      type: string
    - jsonPath: .spec.email
      name: Email
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PocketIDUser is the Schema for the pocketidusers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PocketIDUserSpec defines the desired state of PocketIDUser
            properties:
              disabled:
                default: false
                description: Disabled prevents the user from logging in
                type: boolean
              displayName:
                description: DisplayName for the user (optional, computed from first/last
                  if not set)
                type: string
              email:
                description: Email is the user's email address
                type: string
              firstName:
                description: FirstName of the user
                type: string
              instanceRef:
                description: |-
                  InstanceRef references the PocketIDInstance this user belongs to
                  Can reference instances in other namespaces if allowed by the instance
                properties:
                  name:
                    description: Name of the referenced object
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced object
                      When unspecified, refers to the local namespace
                    type: string
                required:
                - name
                type: object
              isAdmin:
                default: false
                description: IsAdmin grants administrative privileges
                type: boolean
              lastName:
                description: LastName of the user
                type: string
              locale:
                description: Locale for the user (e.g., "en", "de")
                type: string
              oneTimeAccessSecretRef:
                description: |-
                  OneTimeAccessSecretRef specifies the secret to store the one-time access link.
                  Defaults to "<user-cr-name>-onboarding" if not set.
                  Set to empty name to disable automatic onboarding link creation.
                properties:
                  name:
                    description: Name of the referenced object
                    type: string
                required:
                - name
                type: object
              sendOnboardingEmail:
                description: |-
                  SendOnboardingEmail triggers a one-time access email after creation
                  Requires SMTP to be configured on the PocketIDInstance
                type: boolean
              userGroupRefs:
                description: UserGroupRefs references PocketIDUserGroup resources
                  for group membership
                items:
                  description: LocalObjectReference references an object in the same
                    namespace
                  properties:
                    name:
                      description: Name of the referenced object
                      type: string
                  required:
                  - name
                  type: object
                type: array
              username:
                description: Username is the unique username for the user
                minLength: 1
                type: string
            required:
            - instanceRef
            - username
            type: object
          status:
            description: PocketIDUserStatus defines the observed state of PocketIDUser
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the user was last synchronized
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                format: int64
                type: integer
              onboardingEmailSent:
                description: OnboardingEmailSent indicates if the onboarding email
                  was sent
                type: boolean
              onboardingEmailSentAt:
                description: OnboardingEmailSentAt is when the email was sent
                format: date-time
                type: string
              onboardingLinkCreated:
                description: OnboardingLinkCreated indicates if a one-time access
                  link has been created
                type: boolean
              ready:
                description: Ready indicates if the user is synced with PocketID
                type: boolean
              synced:
                description: Synced indicates the user is successfully synchronized
                type: boolean
              userId:
                description: UserID is the PocketID internal user ID
                type: string
            required:
            - ready
            - synced
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
